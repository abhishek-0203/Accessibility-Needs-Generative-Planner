class SpeechAPI:

    @staticmethod
    def transcribe(audio_data):
        return {
            'status': 'placeholder',
            'note': 'Speech-to-text API integration pending.',
            'transcription': None,
            'confidence': None,
        }
